<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LeafCoin Clicker</title>
  <style>
    body {
      background-color: #111;
      color: #eee;
      font-family: Arial, sans-serif;
      text-align: center;
      margin: 0;
    }
    nav {
      background: #222;
      padding: 10px;
    }
    nav button {
      margin: 5px;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      background-color: #444;
      color: #fff;
      cursor: pointer;
    }
    .tab {
      display: none;
    }
    .active {
      display: block;
    }
    #leaf {
      width: 150px;
      transition: transform 0.1s ease;
      cursor: pointer;
    }
    #leaf:active {
      transform: scale(0.9);
    }
    .wheel {
      width: 300px;
      height: 300px;
      border: 10px solid #555;
      border-radius: 50%;
      margin: 20px auto;
      position: relative;
      overflow: hidden;
    }
    .segment {
      position: absolute;
      width: 50%;
      height: 50%;
      background: #666;
      transform-origin: 100% 100%;
      clip-path: polygon(100% 100%, 0 100%, 100% 0);
    }
    #spinBtn {
      margin: 20px;
      padding: 10px 30px;
      font-size: 18px;
    }
  </style>
</head>
<body>
  <h1>LeafCoin Clicker 🌿</h1>
  <nav>
    <button onclick="showTab('clicker')">Кликер</button>
    <button onclick="showTab('roulette')">Рулетка</button>
    <button onclick="showTab('inventory')">Инвентарь</button>
    <button onclick="showTab('shop')">Магазин</button>
  </nav>  <div id="clicker" class="tab active">
    <h2>ЛистКоины: <span id="coins">0</span></h2>
    <img id="leaf" src="leaf.png" onclick="clickLeaf()">
  </div>  <div id="roulette" class="tab">
    <h2>Рулетка</h2>
    <div class="wheel" id="wheel"></div>
    <button id="spinBtn" onclick="spinWheel()">Крутить</button>
    <p>Бесплатная попытка: <span id="freeSpin"></span></p>
  </div>  <div id="inventory" class="tab">
    <h2>Инвентарь</h2>
    <ul id="inventoryList"></ul>
  </div>  <div id="shop" class="tab">
    <h2>Магазин</h2>
    <button onclick="buyAutoclicker()">Автокликер (1 лист/сек) — 1,000,000 листкоинов</button><br>
    <button onclick="buyUpgrade()">Усиление клика (+4/клик) — 2,000,000 листкоинов</button>
  </div>  <script>
    let coins = parseInt(localStorage.getItem('coins')) || 0;
    let clickPower = parseInt(localStorage.getItem('clickPower')) || 1;
    let hasAutoclicker = localStorage.getItem('autoclicker') === 'true';
    let inventory = JSON.parse(localStorage.getItem('inventory')) || [];
    let lastFreeSpin = localStorage.getItem('lastFreeSpin') || 0;

    document.getElementById('coins').innerText = coins;

    function showTab(tabId) {
      document.querySelectorAll('.tab').forEach(tab => tab.classList.remove('active'));
      document.getElementById(tabId).classList.add('active');
    }

    function clickLeaf() {
      coins += clickPower;
      update();
    }

    function buyAutoclicker() {
      if (coins >= 1000000 && !hasAutoclicker) {
        coins -= 1000000;
        hasAutoclicker = true;
        localStorage.setItem('autoclicker', 'true');
        update();
      }
    }

    function buyUpgrade() {
      if (coins >= 2000000 && clickPower === 1) {
        coins -= 2000000;
        clickPower = 4;
        localStorage.setItem('clickPower', clickPower);
        update();
      }
    }

    function update() {
      document.getElementById('coins').innerText = coins;
      localStorage.setItem('coins', coins);
    }

    function saveInventory() {
      localStorage.setItem('inventory', JSON.stringify(inventory));
    }

    function refreshInventory() {
      const list = document.getElementById('inventoryList');
      list.innerHTML = '';
      inventory.forEach(item => {
        const li = document.createElement('li');
        li.textContent = item;
        list.appendChild(li);
      });
    }

    if (hasAutoclicker) {
      setInterval(() => {
        coins += clickPower;
        update();
      }, 1000);
    }

    function spinWheel() {
      const now = new Date().toDateString();
      if (lastFreeSpin !== now) {
        lastFreeSpin = now;
        localStorage.setItem('lastFreeSpin', now);
      } else if (coins >= 100000) {
        coins -= 100000;
      } else {
        alert('Недостаточно коинов и нет бесплатного вращения');
        return;
      }

      const rnd = Math.random() * 100;
      let prize = 0;
      if (rnd < 0.0003) {
        prize = 'Золотой Пепе';
        inventory.push(prize);
        saveInventory();
        refreshInventory();
      } else if (rnd < 11.5003) prize = 2000;
      else if (rnd < 51.5003) prize = 10000;
      else if (rnd < 76.5003) prize = 100000;
      else prize = 500000;

      if (typeof prize === 'number') coins += prize;
      update();
    }

    document.getElementById('freeSpin').innerText = (new Date().toDateString() !== lastFreeSpin) ? 'Доступна' : 'Нет';
    refreshInventory();
  </script></body>
</html>
